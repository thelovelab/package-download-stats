name: Render Quarto site

on:
  workflow_dispatch:  # manual trigger from GitHub UI
  push:
    branches: [main]
    paths:
      - '**.qmd'
      - '**.R'

jobs:
  render:
    runs-on: ubuntu-latest
    container:
      image: bioconductor/bioconductor_docker:RELEASE_3_21

    steps:
      - uses: actions/checkout@v4

      - name: Install Quarto
        run: |
          curl -LO https://github.com/quarto-dev/quarto-cli/releases/latest/download/quarto-linux-amd64.deb
          dpkg -i quarto-linux-amd64.deb

      - name: Install any extra packages
        run: |
          Rscript -e "BiocManager::install(c('BiocPkgTools'), ask=FALSE)"

      - name: Render
        run: quarto render

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./docs
